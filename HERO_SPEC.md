# HERO_SPEC.md

## 1. この課題の目的

実在する美容室サイトの情報構造を参考にしながら、オリジナルのメインビジュアルと特徴カード3件を実装する。

今回確認する能力は次のとおり。

* 見本をHTMLの構造へ分解できる
* モバイルファーストでCSSを書ける
* 390px・768px・1440pxへ対応できる
* 画面幅の途中でも崩れにくくできる
* プレースホルダー画像を本物の画像へ安全に差し替えられる
* AIが変更したコードを確認し、説明できる

---

## 2. 参考サイト

参考サイト：

`https://locohair-web.com/index.html`

参考にするのは、次の情報構造のみ。

* メインビジュアルがある
* メインビジュアルの後に特徴が3件並ぶ
* 各特徴が画像、見出し、本文で構成されている
* PCとスマートフォンで配置が変化する

次の内容はコピーしない。

* 店名
* ロゴ
* 文章
* 写真
* アイコン
* HTML
* CSS
* JavaScript
* スクリーンショット

---

## 3. 実装範囲

今回実装するもの：

1. メインビジュアル
2. 特徴セクションの見出し
3. 特徴カード3件
4. 390px・768px・1440pxへのレスポンシブ対応
5. CTAから特徴セクションへのページ内リンク

---

## 4. 対象外

今回は次を実装しない。

* ヘッダー
* グローバルメニュー
* ハンバーガーメニュー
* 料金表
* スタッフ紹介
* FAQ
* お問い合わせフォーム
* フッター
* JavaScript
* アニメーション
* WordPress
* サーバー公開
* 本番用画像の選定
* 元サイトの完全再現

課題の途中で、対象外の機能を追加しない。

---

## 5. ページの目的

初めてページを訪れた人に、次の印象を伝える。

* 落ち着いて過ごせる美容室
* 一人ひとりに合わせた対応
* 安心して相談できる雰囲気

---

## 6. 想定する利用者

* 近隣で美容室を探している人
* にぎやかな店舗より、落ち着いた空間を好む人
* 自分に合った髪型やケアを相談したい人
* スマートフォンでサイトを見る人

---

## 7. ページ構造

```text
main
├── hero
│   └── container
│       ├── hero__content
│       │   ├── 小見出し
│       │   ├── h1
│       │   ├── 説明文
│       │   └── CTA
│       └── hero__media
│           └── プレースホルダー画像
│
└── features
    └── container
        ├── features__header
        │   ├── 小見出し
        │   └── h2
        └── features__grid
            ├── feature-card
            ├── feature-card
            └── feature-card
```

---

## 8. メインビジュアルの内容

### 小見出し

`HAIR & RELAXATION`

HTML要素：

```html
<p class="hero__eyebrow">
```

### メイン見出し

`髪も心も、軽くなる時間を。`

HTML要素：

```html
<h1 class="hero__title">
```

ページ内の`h1`は、この1件だけにする。

### 説明文

```text
一人ひとりの髪と気持ちに向き合い、
ゆっくり過ごせる時間を提供します。
```

HTML要素：

```html
<p class="hero__lead">
```

### CTA

表示文言：

`私たちの特徴を見る`

リンク先：

`#features`

HTML要素：

```html
<a class="hero__cta" href="#features">
```

ページ内の別セクションへ移動するため、`button`ではなく`a`を使う。

---

## 9. 特徴セクションの内容

### 小見出し

`OUR FEATURES`

### セクション見出し

`私たちの特徴`

HTML要素：

```html
<h2 id="features-title">
```

---

## 10. 特徴カード

### 特徴カード1

見出し：

`経験豊富なスタイリスト`

本文：

```text
髪質や生活スタイルに合わせて、
無理なく続けられるスタイルをご提案します。
```

画像：

`feature-01-placeholder.svg`

---

### 特徴カード2

見出し：

`ゆったり過ごせる空間`

本文：

```text
自然光が入る落ち着いた店内で、
周囲を気にせずゆっくりお過ごしいただけます。
```

画像：

`feature-02-placeholder.svg`

---

### 特徴カード3

見出し：

`一人ひとりに合うメニュー`

本文：

```text
カットやカラーだけでなく、
髪や頭皮の状態に合わせたケアをご案内します。
```

画像：

`feature-03-placeholder.svg`

---

## 11. プレースホルダー画像

使用する画像：

```text
assets/
├── hero-placeholder.svg
├── feature-01-placeholder.svg
├── feature-02-placeholder.svg
└── feature-03-placeholder.svg
```

### メイン画像

* 縦横比：4:3
* 基準サイズ：1200 × 900
* 表示文字：`HERO IMAGE`
* 装飾用なので`alt=""`

### 特徴カード画像

* 縦横比：4:3
* 基準サイズ：800 × 600
* 表示文字：`FEATURE 01`など
* 装飾用なので`alt=""`

### プレースホルダーの目的

* 画像の配置を確認する
* 縦横比を確認する
* レスポンシブ表示を確認する
* 本物の画像が未定でも実装を進める

プレースホルダーの見た目を作り込まない。

---

## 12. 390pxの仕様

モバイル版を基本とする。

### メインビジュアル

表示順：

1. 小見出し
2. メイン見出し
3. 説明文
4. CTA
5. メイン画像

条件：

* 1列
* 左右余白は最低16px
* 横スクロールを発生させない
* 見出しが画面外へ出ない
* CTAが画面幅を超えない
* 画像の縦横比を維持する

### 特徴カード

* 1列
* カード間に十分な余白を置く
* 画像、見出し、本文の順
* 文章量が違っても重ならない
* カードの高さを固定しない

---

## 13. 768pxの仕様

### メインビジュアル

基本案：

* 文章と画像を2列にする
* 文章側と画像側はほぼ同じ幅
* 画像が小さくなりすぎないようにする

例外：

次の場合は1列を維持する。

* 見出しが細かく折り返される
* CTAが窮屈になる
* 画像が小さくなりすぎる
* 文章部分の幅が不足する

ブレークポイントは、端末名ではなく、内容が自然に表示できる位置で決める。

### 特徴カード

* 2列
* 3枚目は次の行の左側
* 3枚目だけ幅を広げない
* すべて同じカード構造を使う

---

## 14. 1440pxの仕様

### ページ全体

* コンテンツ最大幅：約1120px
* 画面全体へ広がりすぎない
* 左右の余白を維持する

### メインビジュアル

* 文章と画像の2列
* 画像を必要以上に大きくしない
* 見出しを必要以上に巨大化させない

### 特徴カード

* 3列
* 3枚の幅を同じにする
* カード間の余白を均等にする

---

## 15. 中間幅の確認

必須確認幅：

* 320px
* 390px
* 480px
* 600px
* 768px
* 900px
* 1024px
* 1200px
* 1440px

確認事項：

* 横スクロールがない
* 文字が重ならない
* 画像が変形しない
* 極端に細い列がない
* 不自然に大きな空白がない
* ブレークポイント付近で急に崩れない

---

## 16. HTML設計ルール

* ページの主見出しには`h1`を使う
* 特徴セクションの見出しには`h2`を使う
* 各カードの見出しには`h3`を使う
* 各特徴カードには`article`を使う
* CTAには`a`を使う
* 小見出しには`p`を使う
* PC用とスマートフォン用でHTMLを複製しない
* 見た目のためだけに不要な要素を増やさない

---

## 17. CSS設計ルール

* モバイルファーストで書く
* FlexboxまたはCSS Gridを使う
* コンテンツ最大幅を設定する
* 画像には`aspect-ratio`と`object-fit`を使う
* 文字サイズや余白には必要に応じて`clamp()`を使う
* 色や最大幅はCSS変数へまとめる
* 3枚のカードは共通クラスで実装する

---

## 18. 禁止事項

次の実装は原則として行わない。

* 元サイトのコードをコピーする
* 元サイトの画像や文章を使う
* PC用とスマートフォン用のHTMLを二重に作る
* 主要レイアウトを`position: absolute`だけで作る
* 文章を収めるために高さを固定する
* 不要な`!important`を使う
* 新しいCSSライブラリを追加する
* JavaScriptを追加する
* 390pxと1440pxだけを合わせて終了する
* AIの変更内容を確認せず採用する

---

## 19. 完了条件

次をすべて満たしたら完成とする。

### 実装

* メインビジュアルがある
* 特徴カードが3件ある
* プレースホルダー画像が4枚表示される
* CTAから特徴セクションへ移動できる

### レスポンシブ

* 320〜1440pxで横スクロールがない
* 390pxで1列表示になる
* 768pxで自然な配置になる
* 1440pxでメインビジュアルが2列になる
* 1440pxで特徴カードが3列になる
* 中間幅でも大きく崩れない

### 品質

* 画像が変形していない
* 文章が重ならない
* 文章量が増えてもカードが壊れない
* コンソールエラーがない
* TabキーでCTAへ移動できる
* CTAのフォーカス表示が見える

### 理解

自分で次を説明できる。

* HTML構造をこの形にした理由
* どの幅でレイアウトを変更したか
* 画像が変形しない仕組み
* 3枚のカードを共通化した方法
* AIが変更したファイルと内容
* 問題が起きた場合にGitで戻す方法

---

## 20. 停止条件

次の場合はCSSを追加し続けず、一度作業を止める。

* 画面幅ごとに別のHTMLが必要になった
* 固定高さを使わないと成立しない
* `position: absolute`が増え続ける
* メディアクエリの上書きを把握できなくなった
* AIが対象外のファイルを変更した
* 元サイトをコピーしないと進められない
* 集中作業8時間を超えても、HTML骨格と390px版が完成しない

停止した場合は、原因を次から選ぶ。

* 参考サイトの構造分析
* HTML設計
* CSSレイアウト
* レスポンシブ設計
* プレースホルダー作成
* AIへの指示
* 育児中断からの復帰

---

## 21. 未確認事項

実装開始時点では、次は未確認。

* 768pxで2列が自然に成立するか
* 適切なブレークポイント
* 見出しの最適な改行位置
* 実際の本番画像を入れた場合の表示
* 完成までの実作業時間
* AI利用によって作業時間が短縮されるか

未確認事項を、推測で「確定仕様」にしない。実際の画面を確認して決める。
